name: Stage Deploy

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

jobs:
  stage:
    uses: Mafbase/Mafbase-workflows/.github/workflows/reusable-stage-deploy.yml@main
    with:
      image_name: seating-generator-web
      service_name: frontend
