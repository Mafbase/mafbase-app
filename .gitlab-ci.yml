# This file is a template, and might need editing before it works on your project.
# This is a sample GitLab CI/CD configuration file that should run without any modifications.
# It demonstrates a basic 3 stage CI/CD pipeline. Instead of real tests or scripts,
# it uses echo commands to simulate the pipeline execution.
#
# A pipeline is composed of independent jobs that run scripts, grouped into stages.
# Stages run in sequential order, but jobs within stages run in parallel.
#
# For more information, see: https://docs.gitlab.com/ee/ci/yaml/index.html#stages
#
# You can copy and paste this template into a new `.gitlab-ci.yml` file.
# You should not add this template to an existing `.gitlab-ci.yml` file by using the `include:` keyword.
#
# To contribute improvements to CI/CD templates, please follow the Development guide at:
# https://docs.gitlab.com/ee/development/cicd/templates.html
# This specific template is located at:
# https://gitlab.com/gitlab-org/gitlab/-/blob/master/lib/gitlab/ci/templates/Getting-Started.gitlab-ci.yml

stages:          # List of stages for jobs, and their order of execution
  - test
  - build
  - deploy

build_job:
  stage: build
  tags:
    - docker
  script:
    - docker build --build-arg "REGISTRY_HOST=$REGISTRY_HOST" -t seating-generator-flutter:latest .
    - docker save seating-generator-flutter:latest > flutter-image.tar
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: push
    paths:
      - flutter-image.tar

code_quality:
  stage: test
  tags:
    - docker
  image: "$REGISTRY_HOST/flutter-builder:3.19.3"
  before_script:
    - flutter pub global activate dart_code_metrics
    - export PATH="$PATH:$HOME/.pub-cache/bin"
  script:
    - metrics lib -r codeclimate  > gl-code-quality-report.json
  artifacts:
    reports:
      codequality: gl-code-quality-report.json

test:
  stage: test
  tags:
    - docker
  image: "$REGISTRY_HOST/flutter-builder:3.16.5"
  before_script:
    - apt-get update
    - apt-get install -y lcov
    - flutter pub global activate junitreport
    - export PATH="$PATH:$HOME/.pub-cache/bin"
  script:
    - flutter test --machine --coverage | tojunit -o report.xml
    - lcov --summary coverage/lcov.info
    - genhtml coverage/lcov.info --output=coverage
  coverage: '/lines\.*: \d+\.\d+\%/'
  artifacts:
    name: coverage
    paths:
      - $CI_PROJECT_DIR/coverage
    reports:
      junit: report.xml

deploy:
  stage: deploy
  image: "$REGISTRY_HOST/deployer:1.0.0"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  tags:
    - docker
  before_script:
    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client git -y )'
    - eval $(ssh-agent -s)
    - echo "$SSH_KEY"
    - ssh-add <(echo "$SSH_KEY")
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - ssh-keyscan -p 22 "$DEPLOY_HOST" >> ~/.ssh/known_hosts
    - chmod 644 ~/.ssh/known_hosts
  script:
    - scp flutter-image.tar "$DEPLOY_USER"@"$DEPLOY_HOST":~/
    - ssh "$DEPLOY_USER"@"$DEPLOY_HOST" "./load_flutter.sh"
    - ssh "$DEPLOY_USER"@"$DEPLOY_HOST" "./restart.sh"
  environment: production
  cache:
    key: "$CI_COMMIT_REF_NAME"
    policy: pull
    paths:
      - flutter-image.tar
